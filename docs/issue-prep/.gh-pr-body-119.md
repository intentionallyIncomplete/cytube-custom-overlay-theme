## Summary

- Merge audio normalization UI into `feature-audio-boost.js` (single `feature:audioboost` boot; boost + norm buttons always on)
- Trust `*.workers.dev` hosts for direct Web Audio (skip CORS proxy)
- Add `workers/vidprox` Cloudflare worker for third-party URL fallback
- Delete `feature-audio-enhancer.js` and remove channel admin `audioEnhancer` integration gate

Closes #119.

## Changes

| Area | Action |
|------|--------|
| `feature-audio-boost.js` | Norm button/menu wired in audioboost; `*.workers.dev` trusted; configurable `CORS_PROXY` |
| `feature-audio-enhancer.js` | Deleted |
| `workers/vidprox/` | New CORS video proxy worker (deploy with `npm run deploy`) |
| `feature-channel-theme-admin.js` | Removed audio enhancer toggle + runtime sync |
| Bundle/boot | Removed enhancer from player bundle and `BTFW.init` |

## Test plan

- [x] `npm test` (64 pass, incl. trusted-host test)
- [x] `npm run build`
- [ ] Deploy vidprox worker: `cd workers/vidprox && npm install && npm run deploy`
- [ ] Direct file on `*.workers.dev`: boost + norm without proxy reload
- [ ] Third-party URL without CORS: proxy fallback works after worker deploy
- [ ] Overlay shows megaphone + waveform buttons on direct media

## Deploy note

Worker must be deployed separately before proxy fallback works in production. Default client URL remains `https://vidprox.billtube.workers.dev/?url=` (override via `BTFW_CONFIG.corsVideoProxy`).
