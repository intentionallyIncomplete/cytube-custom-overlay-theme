## Summary

- Delete `modules/feature-ambient.js` (~485 lines of dead ambient glow code)
- Remove the module from the `player` bundle in `scripts/build.js`
- Rebuild `dist/player.bundle.js`

Closes #118.

## Context

`feature:ambient` was disabled in Oct 2025 (overlay toggle and `BTFW.init` removed). The file remained in the player bundle with no runtime consumers.

## Test plan

- [x] `npm run build` succeeds
- [ ] Player loads on CyTube; direct-file playback unchanged
- [ ] No console errors referencing `feature:ambient` or `btfw:ambient`
